//implementing bottom-top approach
//this defines block to be inserted into layout
//no access to locals in layout

//top bottom would be creating layout first,
//then passing locals to index, that is inserted into layout
extends layout

block content
  h1= title
  p Welcome to #{title}
  div(ng-app="")
  p Name :
    input(type="text" ng-model="name")
  h1 Hello {{name}}
